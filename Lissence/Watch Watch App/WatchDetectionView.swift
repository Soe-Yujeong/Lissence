/// 감지모드

import SwiftUI

struct WatchDetectionView: View {
    var body: some View {
        VStack {
            Text("감지 모드")
                .font(.headline)
            Spacer()
            // 추후 여기에 소리 감지 결과나 텍스트가 표시됩니다.
            Text("소리 대기 중...")
        }
        //.navigationTitle("감지 모드")
    }
}
